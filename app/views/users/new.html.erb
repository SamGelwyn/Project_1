<% provide(:title, "Sign Up") %>

<div class="row">
  <div class="col-md-6 offset-md-3">
    <h1> <%= yield(:title) %> </h1>
    <%= form_for @user do |f| %>
      <%= render 'form', f: f %>
      <%= f.submit "Sign up!", :class => "btn btn-primary" %>
    <% end %>
  </div>
</div>